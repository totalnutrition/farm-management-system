import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Location · Groups & rules" };

export default function LocationGroupsPage() {
  return (
    <ComingSoon
      title="Groups & rules"
      description="Group strategy preset, per-group rules, computed capacity plan."
      note="Lands in PR-C."
    />
  );
}
