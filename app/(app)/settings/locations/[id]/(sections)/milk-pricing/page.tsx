import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Location · Milk pricing" };

export default function LocationMilkPricingPage() {
  return (
    <ComingSoon
      title="Milk pricing"
      description="Effective-dated pricing schemes with correction methods, component bonuses, and country presets."
      note="Lands in PR-L."
    />
  );
}
