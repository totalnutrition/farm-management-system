import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Location · Recording profile" };

export default function LocationRecordingPage() {
  return (
    <ComingSoon
      title="Recording profile"
      description="Test-day frequency, daily recording flag, milkings per day, recording method, bulk tank cadence, component sampling."
      note="Lands in PR-B. Captured during wizard Step 1.5."
    />
  );
}
