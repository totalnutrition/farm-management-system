import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Location · Buyers" };

export default function Page() {
  return (
    <ComingSoon
      title="Buyers"
      description="Milk processors, cull / heifer / calf buyers, manure offtakers. Each buyer can have a default sales contract / pricing scheme."
    />
  );
}
