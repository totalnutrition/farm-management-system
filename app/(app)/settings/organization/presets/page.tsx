import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Organization · Presets" };

export default function OrganizationPresetsPage() {
  return (
    <ComingSoon
      title="Presets"
      description="Group strategy presets, pricing scheme templates, capacity-plan defaults."
    />
  );
}
