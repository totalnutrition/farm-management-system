import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Organization · Billing" };

export default function OrganizationBillingPage() {
  return (
    <ComingSoon
      title="Billing"
      description="Plan, seats, payment method, invoices."
    />
  );
}
