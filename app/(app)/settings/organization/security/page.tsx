import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Organization · Security" };

export default function OrganizationSecurityPage() {
  return (
    <ComingSoon
      title="Security"
      description="Password policy, SSO, MFA enforcement, sessions, IP allowlist."
    />
  );
}
